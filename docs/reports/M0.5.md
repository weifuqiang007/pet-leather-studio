# M0.5 报告 —— 浮雕算法可行线 spike

里程碑：M0.5（PRD v1.1 §3.3 / §15）
日期：2026-09-18
机器：macOS 14.2.1 / Apple silicon (arm64, 8 核, 16GB)

## 实现功能及 AC 编号

| 功能 | AC |
| --- | --- |
| 标注 → 区域基函数高度场 → 封闭 STL 全链路纯脚本 | M0.5 交付定义 |
| 实体宽度误差 ≤0.01mm（网格跨度约定） | AC-F03-03 |
| 输出可重跑：参数快照 + 确定性算法 + 哈希 | §7 校验与可复现要求 |
| 无贴图多视图（真实比例 + 明确标注的 Z×3 夸大版） | §12 相似度评审材料格式 |
| ≥3 合成样本 + 解析检查 + 局部性 + 可复现性自测 | M0.5 通过条件（合成部分） |

代码：`experiments/relief_spike/relief_spike.py`（生产代码不得导入，PRD §15）

## 实际运行命令

```bash
scripts/dev.sh run python experiments/relief_spike/relief_spike.py selftest
scripts/dev.sh run python experiments/relief_spike/relief_spike.py build \
  --annotation experiments/relief_spike/annotations/sample_a_head.json \
  --out experiments/relief_spike/out/sample_a_head
scripts/dev.sh run pytest tests/unit/test_relief_spike.py
```

## 测试结果

selftest `all_pass=true`，明细（`experiments/relief_spike/out/selftest.json`）：

| 样本 | watertight | winding | 体积 mm³ | 宽度误差 | Euler | 面数 | dx (mm) |
| --- | --- | --- | --- | --- | --- | --- | --- |
| sample_a_head（抽象头部 4 区域） | ✓ | ✓ | 14856.5 | 0.0mm | 2 | 1,048,572 | 0.1566 |
| sample_b_bump（单凸起） | ✓ | ✓ | 8207.2 | 0.0mm | 2 | 1,048,572 | 0.1174 |
| sample_c_flat（空区域平板） | ✓ | ✓ | 6250.0 | 0.0mm | 2 | 1,311,716 | 0.0978 |

解析检查：
- 平板体积 6250.0 mm³ == 50×62.5×2.0 解析值（相对误差 <1e-3，非正方形宽高比样本）✓
- 单凸起中心高度与设定值偏差 <0.15mm（网格离散 + 平滑容差内）✓

局部性：鼻部区域 +0.5mm 扰动 → 4928 个网格点发生变化，**影响带外泄漏 0 点**
（影响带 = 区域权重 >0.01 外扩 3 网格像素）。

可复现性：同参数两次构建 STL sha256 完全一致
（`c413730e…cb0fa4`），单样本端到端（含渲染 6 视图）耗时约 11–14s。

单元测试：`tests/unit/test_relief_spike.py` 6 项全过（平板解析体积、Y 翻转、
边界拒绝、字节级重跑一致）。

渲染：18 张视图（3 样本 × front/oblique/side × 真实比例/Z×3）全部由
pyvista 离屏（VTK 9.7.0）生成，未触发 matplotlib 回退。已人工目检
`sample_a_head` 斜视图：矩形基板 + 平滑凸起区域、光照正常、无破面。

## 手工演示步骤

1. `scripts/dev.sh run python experiments/relief_spike/relief_spike.py selftest`
   —— 终端输出 `all_pass: true` 与各检查明细。
2. 打开 `experiments/relief_spike/out/sample_a_head/views/oblique.png`（真实比例）
   与 `oblique_zx3.png`（夸大版）对比观察起伏。
3. 将 `out/sample_a_head/relief_solid.stl` 拖入任意切片器（如 OrcaSlicer），
   确认可加载、水密、尺寸 80×80×(2+凸起) mm 量级。
4. 修改 `annotations/sample_a_head.json` 中任一 `height_mm` 后重跑 build，
   对比 `validation.json` 体积变化，验证参数可重跑。

## 新增依赖/模型及本地路径

无新增。复用 M0 锁定依赖（numpy/scipy/trimesh/Pillow/pyvista，均项目本地
.venv）。无任何模型下载、无网络访问。

## 真实能力与模拟能力

- **真实**：多边形标注 → 平滑权重 → 叠加高度场 → 水密实体网格 → STL/视图，
  全部数值验收通过；几何管线本身可行（M0.5 要回答的问题）。
- **模拟/非真实**：3 个样本的"标注"是手写合成多边形，**不是从照片提取**；
  照片→标注环节（轮廓/区域识别）本里程碑未实现，属 M1+ 范围。
- 相似度评审结论：**pending**——见 `experiments/relief_spike/out/REVIEW-PACKAGE.md`，
  需用户提供猫/短毛犬/长吻犬真实照片各 ≥1 张。

## 未通过项、原因和后续处理

| 项 | 状态 | 原因与处理 |
| --- | --- | --- |
| 相似度人工评审（PRD §12.4） | **pending** | 缺真实照片；已列出所需材料清单，待用户提供后补做，不用合成样本代评 |
| 照片→标注环节 | 未实现 | 按 PRD 属 M1+ 交付，M0.5 仅验证"标注→STL"后段链路 |
| Blender/FreeCAD 侧验证 | 未做 | 按用户指示使用已有 Blender、暂不装 FreeCAD；STL 已用切片器可读性说明替代，M1 接入 Blender 时补 |
