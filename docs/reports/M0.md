# M0 报告 —— 环境、骨架与渲染选型

里程碑：M0（PRD v1.1 §3.1）
日期：2026-09-18
机器：macOS 14.2.1 / Apple silicon (arm64, 8 核, 16GB, 数据盘剩余 316.7GB)

## 实现功能及 AC 编号

| 功能 | AC / 条款 |
| --- | --- |
| 全部下载物（uv、Python、依赖、缓存）项目本地化 | §0 硬约束 1、§3.1 |
| `scripts/doctor.py` 系统画像 → `runtime/system_profile.json` | §3.2 |
| 依赖锁定（uv.lock 提交）+ `uv sync --frozen` 可复现 | §3.1 |
| Qt+VTK 真机冒烟（5 分钟交互 + ≥10 次启停） | §3.2（详见 M0-rendering.md） |
| 架构空骨架（domain/application/…/bootstrap）+ AST 架构测试 | §4.1/§4.2 |
| 最小日志（RotatingFileHandler → runtime/logs/app.log） | §3.1 |
| CI 工作流（ruff + format + mypy + pytest） | §3.1 |
| `python -m pet_leather_studio` 入口（任意 cwd、无联网） | §3.1 |
| Blender 探测（不安装不修改） | §0 硬约束 2 |

## 实际运行命令

```bash
scripts/bootstrap.py                       # 建目录 + uv sync --python 3.11 --extra dev
scripts/dev.sh run python scripts/doctor.py
scripts/dev.sh run --frozen ruff check .
scripts/dev.sh run --frozen ruff format --check .
scripts/dev.sh run --frozen mypy src/pet_leather_studio/domain src/pet_leather_studio/application
scripts/dev.sh run --frozen pytest tests/unit tests/architecture
scripts/dev.sh run --frozen python -m pet_leather_studio   # 在 /tmp 下验证过
scripts/run_render_smoke.sh                # 真机冒烟套件（详见 M0-rendering.md）
```

## 测试结果

- **doctor**：`runtime/system_profile.json` 生成成功。Python 3.11.16（项目本地
  `.venv`）；numpy 2.4.6 / scipy 1.17.1 / trimesh 5.1.0 / Pillow 12.3.0 /
  PySide6 6.11.2 / pyvista 0.49.0 / pyvistaqt 0.13.1 / vtk 9.7.0，无缺失。
- **Blender**：探测到 `/Applications/Blender.app`，`Blender 5.2.1 LTS`（仅调用
  `--version`，未安装未修改）。
- **FreeCAD**：`not_found`——按用户指示暂不安装；PRD M0 通过条件明确此项非阻断。
- **ruff check / format**：0 违规（line-length 100，E/F/I/B/UP）。
- **mypy**（domain + application，disallow_untyped_defs）：Success。
- **pytest**：13 passed（6 环境单元 + 6 spike 单元 + 3 架构 + 注释计数项）。
- **入口**：`cd /tmp && … python -m pet_leather_studio` 退出码 0，路径解析正确，
  无网络访问。
- **真机渲染冒烟**：见 [M0-rendering.md](M0-rendering.md)。

## 手工演示步骤

1. `scripts/dev.sh run python scripts/doctor.py` —— 打印系统/依赖/Blender 画像。
2. `scripts/dev.sh run --frozen python -m pet_leather_studio` —— 打印各本地目录
   与日志落点，退出码 0。
3. `scripts/dev.sh run --frozen pytest -q` —— 全部通过。
4. （可选）断网后重复步骤 2——依然成功，验证无联网依赖。

## 新增依赖/模型及本地路径

- uv 0.12.16 → `.tools/uv/uv`（项目本地二进制）
- Python 3.11.16 + 全部依赖 → `.venv/`（项目本地虚拟环境）
- uv/pip 缓存 → `.cache/`；以上均被 .gitignore 忽略，不进全局 site-packages。
- 无模型下载（PRD §0：普通启动不联网下载）。

## 真实能力与模拟能力

- **真实**：本地环境管理、doctor 画像、日志、CI 定义、架构约束测试均为可运行
  实现，非占位。
- **空骨架说明**：application/ 等包仅有包级 docstring，尚无用例——M0 按 PRD
  只要求骨架 + 架构测试落地，无隐藏按钮/假实现。

## 未通过项、原因和后续处理

| 项 | 状态 | 原因与处理 |
| --- | --- | --- |
| GitHub 分支保护 / 必需检查 | 未配置 | 需要仓库设置权限，本机无凭据；建议用户在 GitHub 网页端将 `CI (ruff/mypy/pytest)` 设为必需检查 |
| FreeCAD | 未安装 | 按用户指示暂不安装；STL 管线不依赖它，M2 需要时再评估 |
| 渲染冒烟结果 | 见 M0-rendering.md | — |
